const db = require('../models/model');

const eventController = {};


eventController.createEvent = (req, res, next) => {
    try {

    } catch {

    }

}
// After you log in to the platform, you would get an invitation list
eventController.getInvitations = (req, res, next ) => {
    
}

eventController.createEvent = (req, res, next) => {


}